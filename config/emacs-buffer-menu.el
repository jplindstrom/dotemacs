

;; List buffers
;;;JPL
(load "ebuff-menu" t)
;; (load "buff-menu+" t)
;; (defun buffer-menu-same-window ()
;;    "buffer-menu, but in the same window"
;;    (interactive)
;;    (other-window 0)
;;    (buffer-menu nil)
;;    )
;; (global-set-key (kbd "C-x C-b") 'buffer-menu-same-window)
(global-set-key "\C-x\C-b" 'buffer-menu)




