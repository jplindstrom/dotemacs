
;; Load CEDET
;; (setq load-path (cons (concat emacs-home-directory "elisp/cedet-1.0pre3/cogre") load-path))
;; (setq load-path (cons (concat emacs-home-directory "elisp/cedet-1.0pre3/contrib") load-path))
;; (setq load-path (cons (concat emacs-home-directory "elisp/cedet-1.0pre3/ede") load-path))
;; (setq load-path (cons (concat emacs-home-directory "elisp/cedet-1.0pre3/eieio") load-path))
;; (setq load-path (cons (concat emacs-home-directory "elisp/cedet-1.0pre3/semantic") load-path))
;; (setq load-path (cons (concat emacs-home-directory "elisp/cedet-1.0pre3/speedar") load-path))
;; (load-file (concat emacs-home-directory "elisp/cedet-1.0pre3/common/cedet.el"))
;; (semantic-load-enable-code-helpers)


;; (setq load-path (cons (concat emacs-home-directory "elisp/ecb") load-path)))
;; (require 'ecb)


